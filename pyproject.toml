[project]
name = "hello-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=3.16.2",
    "python-dotenv>=1.2.3",
    "serpapi>=1.1.1",
    "tavily>=1.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
